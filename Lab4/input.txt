// ќшибка: использование необъ€вленной переменной
int main() {
    int x = 5;
    y = x + 10; // —емантическа€ ошибка: 'y' не объ€влена
    
    // ќшибка в условии while
    while (undefinedVar > 0) { // 'undefinedVar' не объ€влена
        x = x - 1;
    }
    
    return 0;
}